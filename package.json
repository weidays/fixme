{
  "name": "fixme-vip",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "generate": "bun scripts/generate-code.ts",
    "generate-batch": "tsx scripts/generate-batch.ts"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.4.1",
    "astro": "^5.13.2"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
